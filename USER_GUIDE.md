# StatAppR v2.0.0-alpha ユーザーガイド

## 📖 目次
1. [概要](#概要)
2. [インストール](#インストール)
3. [基本操作](#基本操作)
4. [パラメータについて](#パラメータについて)
5. [分析結果の確認](#分析結果の確認)
6. [トラブルシューティング](#トラブルシューティング)

---

## 概要

StatAppR は、統計分析を簡単に実行できるデスクトップアプリケーションです。
CSV ファイルを読み込んで、32 種類の統計分析からお好きな分析を選択して実行できます。

**特徴**:
- ✨ R の知識がなくても使える
- 🚀 自動的に必須パッケージをインストール
- 📊 美しいグラフと統計値を自動生成
- 💾 結果は `~/Documents/StatAppR/` に自動保存

---

## インストール

### システム要件
- macOS 15.6 以上
- R 3.5.0 以上（未インストール時は自動インストール可能）

### 手順

1. **StatAppR をダウンロード**
   - アプリケーションフォルダに配置

2. **初回起動時**
   - R パッケージ管理ウィンドウが自動開放
   - R の状態を確認

3. **R をインストール（必要な場合）**
   - 「R パッケージ管理」→「R 環境」タブ
   - R がインストールされていない場合、「インストール」ボタンをクリック
   - Homebrew を使用して自動インストール

---

## 基本操作

### ステップ1: データの準備

CSV ファイルを用意してください。形式:
```
年齢,性別,治療法,スコア
25,M,A,78
31,F,A,82
...
```

**要件**:
- 1行目にカラムヘッダを記載
- カラム区切りは **カンマ（,）**
- 文字エンコーディング: UTF-8 推奨

### ステップ2: アプリ起動

StatAppR を起動すると、ウェルカム画面が表示されます。

```
┌─────────────────────────────────┐
│ StatAppRへようこそ               │
│                                   │
│ CSVデータの読み込み              │
│ データをCSVファイルから読み込みます│
│                                   │
│ レシピの選択                     │
│ 実行したい統計分析を選びます      │
│                                   │
│ 分析の実行                       │
│ 分析を実行して結果を確認します   │
└─────────────────────────────────┘
```

### ステップ3: 分析カテゴリを選択

左サイドバーから分析カテゴリを選択します：

- **基本統計**: 記述統計、平均値、中央値など
- **相関・回帰**: 相関分析、線形回帰、ロジスティック回帰
- **群比較**: t検定、ANOVA、Mann-Whitney U検定
- **時間解析**: Kaplan-Meier曲線、Cox回帰
- **メタアナリシス**: 複数研究のメタアナリシス
- その他多数

### ステップ4: レシピを選択

カテゴリ内のレシピ一覧から、実行したい分析をクリック。

```
┌────────────────────────────────────┐
│ 基本統計（基本統計分析）            │
├────────────────────────────────────┤
│ レシピ1: 記述統計分析              │
│ 説明: 平均、中央値、標準偏差...    │
│ 必要な列: 数値列                   │
│                                    │
│ レシピ2: カテゴリ分析              │
│ 説明: 度数分布、クロス集計...      │
│ 必要な列: カテゴリ列               │
└────────────────────────────────────┘
```

### ステップ5: CSV を読み込む

「データの読み込み」ボタンをクリックして CSV ファイルを選択。

✅ ファイル読み込み成功後：
- ファイル情報が表示される
- カラム数、行数が自動表示
- 必須パラメータが自動マッチングされる

### ステップ6: パラメータを設定

自動マッチされたパラメータを確認：

```
パラメータ設定
├─ [?] 数値列
│  └─ (自動選択: age, score など)
├─ [?] グループ列
│  └─ (自動選択: treatment, group など)
└─ [?] カテゴリ列
   └─ (自動選択: gender, category など)
```

**[?] アイコンについて**: マウスをホバーするとパラメータの説明が表示されます。

**パラメータを手動で変更**:
- 列選択ボックスをクリック
- 必要な列をチェック（単数または複数選択）

### ステップ7: 分析を実行

「分析を実行」ボタンをクリック。

```
実行中... ⏳
分析処理が実行されます（数秒～1分）
```

✅ 分析完了後：
- 統計値がテーブルで表示される
- グラフが自動生成される
- すべての結果が `~/Documents/StatAppR/` に保存される

---

## パラメータについて

### [?] アイコンの使い方

各パラメータ名の横に青い **[?]** アイコンがあります。

1. マウスを [?] の上に移動させる
2. 2-3秒でツールチップが表示される
3. パラメータの説明が表示される

### よく使うパラメータ

| パラメータ | 説明 | 例 |
|----------|------|-----|
| **outcome_column** | 分析対象の結果変数 | disease_status, outcome |
| **treatment** | 処置・施策を示す変数 | treatment, group (0/1) |
| **covariates** | 調整する背景変数 | age, gender, baseline_score |
| **time_column** | イベント発生までの時間 | days, months |
| **event_column** | イベント発生の有無 | event (0/1) |

### パラメータ選択のヒント

**数値型パラメータ**（連続変数）:
- 年齢、スコア、身長、体重 など
- 通常オレンジ色で表示

**カテゴリ型パラメータ**（離散変数）:
- 性別、処置法、グループ など
- 通常青色で表示

**複数選択が可能な場合**:
- チェックボックスで複数選択可能
- 例: 複数の予測変数を同時に使用

---

## 分析結果の確認

### 結果ファイルの保存場所

すべての分析結果は自動的に保存されます：

**デフォルト**: `~/Documents/StatAppR/`

**確認方法**:
1. 「R パッケージ管理」→「R 環境」タブ
2. 「📁 ワークディレクトリ」セクション
3. フォルダアイコンをクリックして Finder で表示

### 保存されるファイル

```
~/Documents/StatAppR/
├── recipe_output_12345.json     # 統計値（JSON形式）
├── plot_001.png                 # グラフ1（PNG形式）
├── plot_002.png                 # グラフ2（PNG形式）
└── ...
```

### 結果の見方

**テーブル表示** (アプリ内):
- 統計値が直感的にテーブルで表示される
- p値、信頼区間、効果量 など

**グラフ** (PNG ファイル):
- 箱ひげ図、散布図、ヒストグラム、森プロット など
- Finder で開いて確認、他のアプリにコピペ可能

---

## ワークディレクトリの設定

### 標準設定

デフォルトでは `~/Documents/StatAppR/` に設定されています。
この場所は「書類」フォルダ内で、Finder から簡単にアクセスできます。

### カスタム位置に変更

1. 「R パッケージ管理」→「R 環境」タブを開く
2. 「📁 ワークディレクトリ」セクション
3. 「変更」ボタンをクリック
4. フォルダ選択ダイアログで好きな場所を選択
5. 「開く」をクリック

✅ 設定は自動保存されます（次回起動時も同じ場所を使用）

---

## トラブルシューティング

### Q1: "R が見つかりません" と表示される

**原因**: R がインストールされていない

**解決方法**:
1. 「R パッケージ管理」→「R 環境」タブ
2. 「インストール」ボタンをクリック
3. Homebrew が自動インストールされる
4. R が自動インストールされる（3-5分かかります）

### Q2: CSV ファイルが読み込めない

**確認事項**:
- ✅ ファイルは CSV 形式か（拡張子 .csv）
- ✅ 1行目にカラムヘッダがあるか
- ✅ エンコーディングが UTF-8 か
- ✅ 区切り文字がカンマ（,）か

**解決方法**:
```
例: 不正な形式
名前|年齢|性別  ❌ (パイプ区切り)
A|25|M

例: 正しい形式
名前,年齢,性別  ✅ (カンマ区切り)
A,25,M
B,31,F
```

### Q3: 分析が失敗する

**確認事項**:
1. CSV ファイルの形式は正しいか
2. パラメータが正しく設定されているか
3. データ型が一致しているか（数値列は数値など）

**解決方法**:
- エラーメッセージを確認
- パラメータ設定を見直す
- CSV ファイルの内容を確認

### Q4: グラフが表示されない

**原因**: 分析は成功したが、グラフ生成に失敗

**解決方法**:
1. Finder で `~/Documents/StatAppR/` を開く
2. .png ファイルが存在するか確認
3. アプリを再起動してみる

---

## 便利な使い方

### 複数の分析を実行

1. 同じ CSV ファイルで複数の分析を実行可能
2. 「別のレシピを選択」ボタンで別の分析に切り替え
3. パラメータを調整して再実行

### 結果をレポートに含める

1. `~/Documents/StatAppR/` から PNG ファイルをコピー
2. Word、PowerPoint、Google Docs などに貼り付け
3. テーブルは手動でコピペするか、JSON ファイルを参照

### JSON ファイルの活用

`.json` ファイルには詳細な統計値が含まれています：
- テキストエディタで開いて確認
- Python/R スクリプトで自動処理可能
- データベースにインポート可能

---

## 推奨パラメータについて

StatAppR の **推奨パラメータ** 機能は、CSV のカラムヘッダを分析して自動的に適切なパラメータを提案します。

**推奨パラメータのマッチング例**:

| CSV カラムヘッダ | 自動マッチ結果 |
|-----------------|--------------|
| treatment | 処置変数 ✅ |
| age | 共変量（年齢） ✅ |
| outcome | 目的変数 ✅ |
| baseline_score | 共変量（ベースライン） ✅ |
| group | グループ変数 ✅ |

**カスタムヘッダも認識**:
- "treat", "trt" → 処置変数と認識
- "result", "outcome" → 目的変数と認識
- "gender", "sex" → カテゴリ変数と認識

---

## 📞 よくある質問

**Q: 複数の CSV ファイルを同時に分析できますか？**
A: いいえ、一度に1つの CSV ファイルのみ処理します。複数ファイルはアプリを再起動して実行してください。

**Q: R スクリプトをカスタマイズできますか？**
A: 現在のアルファリリースではできません。将来のバージョンで予定しています。

**Q: macOS 以外で使えますか？**
A: 現在は macOS のみです。Windows 版は将来検討予定です。

**Q: 分析結果を他の人と共有できますか？**
A: はい。PNG ファイルをメール添付やクラウドストレージで共有可能です。JSON ファイルも共有できます。

---

## 📚 参考資料

- **R 公式サイト**: https://www.r-project.org/
- **統計学の基礎**: 各レシピのヘルプ [?] アイコンを参照
- **CSV ファイル形式**: https://tools.ietf.org/html/rfc4180

---

**StatAppR をご利用いただきありがとうございます！**

ご不明な点やフィードバックがあれば、お気軽にお知らせください。

# StatAppR v2.0.0-alpha リリースノート

**リリース日**: 2026年3月8日
**バージョン**: 2.0.0-alpha
**プラットフォーム**: macOS 15.6 以上

---

## 🎉 新機能

### UI/UX の大幅改善
- **フォント拡大**: 全画面で25-40%のフォント拡大により読みやすさ向上
- **レイアウト改善**: レシピ選択ヘッダーを中央寄せ、詳細説明枠をさらに横長に
- **パラメータ用語説明**: 各パラメータ名の横に「？」アイコン→ホバーで用語説明表示（60+定義）

### ワークディレクトリ管理
- **デフォルト位置**: `~/Documents/StatAppR/` に自動設定
- **設定変更**: 「R パッケージ管理」→「R 環境」タブで別の場所に変更可能
- **簡単アクセス**: Finderボタンで直接フォルダを開ける

### 推奨パラメータ機能
- **自動パラメータマッチング**: CSV 読み込み時に最適なパラメータを自動選択
- **32個のレシピ対応**: すべての分析レシピで推奨パラメータをサポート

---

## 📋 主な改修内容

### フェーズ1-5: タイポグラフィ強化
- WelcomeView フォント増大
- レシピ選択ヘッダー中央寄せ化
- 詳細説明ボックス拡張
- パラメータ設定画面全体のフォント増大
- 列選択行のフォント拡大（`.caption2` → `.body`）
- レシピカードの視認性向上

### フェーズ6: パラメータ用語集
- **新ファイル**: `ParameterGlossary.swift` (60+定義)
- **UI統合**: 各パラメータ名の横に青い「？」アイコン
- **ホバーツールチップ**: マウスをホバーするとパラメータの説明を表示

### ワークディレクトリシステム
- `NSTemporaryDirectory()` から `~/Documents/StatAppR/` に変更
- UserDefaults で設定を保存
- フォルダ選択ダイアログで柔軟に変更可能
- 自動ディレクトリ作成と検証

---

## 🔧 技術仕様

### 対応OS
- macOS 15.6 以上

### システム要件
- **R**: 3.5.0 以上（自動インストール可能）
- **Homebrew**: R インストール時に必要（オプション）

### 分析機能
- **32個の統計レシピ** 対応
- **自動パッケージインストール**: jsonlite, ggplot2 など必要なパッケージを自動インストール
- **推奨パラメータ**: CSV カラムから最適なパラメータを自動提案

### ファイル形式
- **入力**: CSV（UTF-8 推奨）
- **出力**:
  - 図表・グラフ（PNG形式）
  - 解析結果データ（JSON形式）
  - 統計値・テーブル

---

## 💡 ユーザーガイド（簡易版）

### セットアップ
1. StatAppR をダウンロード・起動
2. 「R パッケージ管理」ウィンドウで R の状態を確認
3. R がインストールされていない場合は「インストール」ボタンをクリック

### 基本的な使用方法
1. **データ準備**: CSV ファイルを用意（カラムヘッダ必須）
2. **カテゴリ選択**: 左サイドバーから分析カテゴリを選択
3. **レシピ選択**: 実行したい分析レシピをクリック
4. **CSV 読み込み**: 「データの読み込み」ボタンで CSV ファイルを選択
5. **パラメータ設定**:
   - 推奨パラメータが自動選択される
   - 各パラメータの「？」アイコンで説明を確認
   - 必要に応じて手動調整
6. **分析実行**: 「分析を実行」ボタンをクリック
7. **結果確認**:
   - 統計値がテーブルで表示される
   - グラフが自動生成される
   - 結果は自動的にワークディレクトリに保存される

### ワークディレクトリへのアクセス
1. 「R パッケージ管理」→「R 環境」タブを開く
2. 「📁 ワークディレクトリ」セクションで保存場所を確認
3. フォルダアイコンをクリックして Finder で確認
4. 「変更」ボタンで別の場所を選択可能

---

## 🐛 既知の問題

- なし（アルファリリース）

---

## 📝 変更履歴

### v2.0.0-alpha (2026-03-08)
- **新規**: UI 全体の大幅改善（フォント拡大・レイアウト改善）
- **新規**: パラメータ用語集（60+定義、ホバーツールチップ表示）
- **新規**: ワークディレクトリ設定システム（デフォルト + カスタマイズ可能）
- **改善**: パラメータ自動マッチング精度向上
- **改善**: R パッケージ自動インストール機能
- **改善**: CSV auto-loading for recommended parameters

### v1.0 (2026年初期)
- 初版リリース

---

## 📞 サポート情報

### R のインストール

**方法1: Homebrew を使う（推奨）**
```bash
brew install r
```

**方法2: CRAN から直接インストール**
- https://cran.r-project.org にアクセス
- macOS 用 R をダウンロード・インストール

### トラブルシューティング

**R が見つからない場合**
1. ターミナルで `which Rscript` を実行して確認
2. インストール位置を確認後、アプリを再起動

**分析エラーが発生した場合**
1. CSV ファイルのフォーマットを確認（カラムヘッダ必須、UTF-8推奨）
2. データ型が正しいか確認（数値列は数値、カテゴリ列は文字列など）
3. 必須パラメータがすべて設定されているか確認

---

## ✅ テスト済み機能

- ✅ 基本統計分析
- ✅ ロジスティック回帰
- ✅ 2群比較（連続変数）
- ✅ カイ二乗検定
- ✅ メタアナリシス
- ✅ サバイバル分析
- ✅ その他 26 個の統計レシピ

---

## 🎯 フィードバック

アルファリリースのため、以下の点についてフィードバックをお願いします：

1. **UI/UX**: レイアウト、フォントサイズ、わかりやすさ
2. **パラメータ説明**: 用語集の内容は理解しやすいか
3. **ワークディレクトリ**: 設定方法は直感的か
4. **分析機能**: 結果は正確か、期待通りか
5. **パフォーマンス**: 動作は快適か

---

**StatAppR v2.0.0-alpha へのアップグレードをお待たせしました。**
**皆様のご意見・ご提案をお待ちしています！**

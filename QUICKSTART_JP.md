# StatAppR - クイックスタートガイド

**StatAppRへようこそ!** 📊

このガイドでは、アプリを使って初めての統計分析を実行するまでの手順を説明します。
**5分で完成する簡単な例**をご紹介します。

---

## 📥 インストール

### ステップ 1: アプリをダウンロード

```
StatAppR-v2.0.dmg をダウンロード
  ↓
ダブルクリックして開く
  ↓
StatAppR.app をアプリケーションフォルダにドラッグ
  ↓
アプリケーションフォルダから StatAppR をダブルクリック
```

### ステップ 2: Rのインストール確認

StatAppRを起動すると、R環境を自動検出します。

**Rがインストールされていない場合**:
```bash
# ターミナルで以下を実行
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
brew install r
```

---

## 🚀 初めての分析 (5分でできる!)

### 例1: 患者データの統計分析 (基本統計)

#### Step 1: サンプルデータをダウンロード

ここをクリック → `1_BasicStats_patient_demographics.csv`

```csv
patient_id,age,weight_kg,height_cm,systolic_bp,diastolic_bp,cholesterol_mg_dl,glucose_mg_dl
P001,45,72.5,170,128,82,195,105
P002,52,68.3,165,135,88,210,112
... (合計12患者)
```

#### Step 2: アプリを開く

1. StatAppRを起動
2. 左パネルで **📊 基本統計** を選択

#### Step 3: データをロード

1. 左下の **[📁 CSVをロード]** をクリック
2. `1_BasicStats_patient_demographics.csv` を選択
3. **打ち込み完了!** メインエリアに列情報が表示されます

#### Step 4: 分析を実行

1. メインエリアで **Descriptive Statistics** を選択
2. **[分析を実行]** をクリック
3. **15秒待つ...**

#### Step 5: 結果確認

以下のような統計量が表示されます:

```
分析完了! ✅

基本統計量:
───────────────────────────────
年齢（age）:
  平均値: 49.8歳
  標準偏差: 6.2
  最小値: 38歳
  最大値: 61歳

体重（weight_kg）:
  平均値: 75.2 kg
  標準偏差: 4.1 kg

...（その他の変数）
───────────────────────────────
```

---

### 例2: 治療の効果比較 (グループ比較)

#### Step 1: サンプルデータをダウンロード

`2_GroupComparison_treatment_vs_control.csv`

#### Step 2: データを読み込む

1. 左パネルで **⚖️ グループ比較** を選択
2. **[CSVをロード]** で治療データを選択

#### Step 3: 分析を選択

3つの選択肢が表示されます:

- ✅ **T-Test (Independent)** - 2グループの比較 ← まずはこれ！
- ANOVA - 3グループ以上の比較
- Mann-Whitney U Test - ノンパラメトリック検定

**T-Test** を選択 → **[分析を実行]**

#### Step 4: 結果の見方

```
T検定結果
───────────────────────────────
帰無仮説: 2グループの平均は同じ
対立仮説: 2グループの平均は異なる

検定統計量: t = 12.45
p値: < 0.001 ← 重要!

p値の解釈:
  p < 0.05 → 有意差あり ✅ (差がある!)
  p > 0.05 → 有意差なし (差がない)

この場合: p < 0.001 ← 強い証拠で差がある!
───────────────────────────────
```

---

### 例3: 価格予測モデル (回帰分析)

#### Step 1: サンプルデータをダウンロード

`3_Regression_house_price_prediction.csv`

#### Step 2: 分析開始

1. 左パネルで **📈 回帰分析** を選択
2. `3_Regression_*.csv` をロード
3. **Multiple Regression** を選択

#### Step 3: 結果の解釈

```
重回帰モデル結果
───────────────────────────────
結果変数: price_usd (住宅価格)

係数:
  size_sqft: 150.25 ← 面積が1平方フィート増えると
                    → 価格が $150 上昇

  bedrooms: 25000    ← 寝室が1つ増えると
                    → 価格が $25,000 上昇

  age_years: -2500   ← 築年数が1年増えると
                    → 価格が $2,500 低下

R² = 0.87 ← モデルの予測精度は87%
───────────────────────────────
```

---

## 📊 データタイプ別ガイド

### 1️⃣ 基本統計 (記述統計)

**こんなときに使う**:
- データの中心や広がりを知りたい
- 変数間の相関を調べたい
- 外れ値を見つけたい

**データ形式**:
```csv
patient_id,age,bmi,blood_pressure
P001,45,24.5,128
P002,52,26.1,135
```

**出力**:
- 平均値、中央値、標準偏差
- 最小値、最大値、四分位数
- 相関係数行列 (複数変数の場合)

---

### 2️⃣ グループ比較

**こんなときに使う**:
- 2つ以上のグループを比較したい
- 治療効果があるかを検定したい
- 男性と女性で差があるかを調べたい

**データ形式**:
```csv
patient_id,group,treatment,score
P001,treatment,yes,85
P002,control,no,72
```

**選択可能な検定**:
- ✅ T-Test (正規分布の場合)
- ✅ Mann-Whitney U (非正規分布の場合)
- ✅ ANOVA (3グループ以上)

---

### 3️⃣ 回帰分析

**こんなときに使う**:
- 1つ以上の変数から値を予測したい
- 影響を与える要因を特定したい
- 変数間の関係を定量化したい

**データ形式**:
```csv
house_id,price,size,bedrooms,age
H001,350000,2000,3,15
H002,425000,2500,4,8
```

**出力**:
- 係数（各要因の効果）
- 標準誤差とt値
- p値（有意性）
- R²（予測精度）

---

### 4️⃣ 時系列・パネルデータ

**こんなときに使う**:
- 企業の売上の時間的変化を分析したい
- 複数の企業・地域を同時に追跡したい
- 政策導入前後の効果を比較したい (DiD)

**データ形式**:
```csv
company_id,year,sales,marketing_spend
C001,2023,125000,15000
C001,2024,210000,28000
```

**分析方法**:
- トレンド分析（時間による変化）
- 固定効果モデル（個体間の違いを制御）
- 差分の差推定 (DiD) - 政策評価用

---

### 5️⃣ 生存分析

**こんなときに使う**:
- 患者の生存期間を分析したい
- イベント発生までの時間を予測したい
- 治療群間の生存曲線を比較したい

**データ形式**:
```csv
patient_id,time_months,event_occurred,treatment
S001,12,1,A
S002,24,0,B  # 0=生存継続, 1=イベント発生
```

**出力**:
- Kaplan-Meier生存曲線
- 生存率（1年、3年、5年など）
- ハザード比（危険度）
- Cox比例ハザードモデル結果

---

### 6️⃣ 因果推論

**こんなときに使う**:
- 政策や介入の因果効果を推定したい
- セレクション・バイアスを制御したい
- 異質な処置効果を分析したい (Causal Forest)

**データ形式**:
```csv
id,treatment,outcome,age,education
1,1,45000,28,14
2,0,32000,25,12
```

**分析方法**:
- マッチング（類似した個体をペアリング）
- 傾向スコア（バイアス調整）
- DML（ダブル機械学習）
- Causal Forest（個別効果の推定）

---

### 7️⃣ 次元削減

**こんなときに使う**:
- 10個以上の変数を2-3個に圧縮したい
- 共通の背景因子（潜在因子）を抽出したい
- 多くの特徴量からパターンを見つけたい

**データ形式**:
```csv
sample_id,gene_1,gene_2,...,gene_10
S001,2.4,5.8,3.2,4.1,...
S002,1.8,6.2,2.9,4.5,...
```

**分析方法**:
- PCA（主成分分析） - 分散最大化
- PLS（部分最小二乗法） - 予測最適化
- 因子分析 - 潜在因子抽出

---

## ❓ よくある質問

### Q: "計算に失敗しました" というエラーが出た

**A:** 以下を確認してください:

1. **CSV形式が正しい?**
   - ヘッダー行がある (必須)
   - 各行の列数が同じ
   - 数値列に数字のみ

2. **データが足りない?**
   - 基本統計: 最小3行
   - グループ比較: 各グループ最小6行
   - 回帰分析: 最小12行

3. **選択が正しい?**
   - 「グループ比較」でグループ列を選択したか?
   - 「生存分析」でイベント列を選択したか?

---

### Q: 大きいCSV (10000行) でも大丈夫?

**A:** ほぼ大丈夫です。ただし:

- 読み込み: 10-20秒
- 分析実行: 10-30秒 (分析の複雑さによる)
- メモリ: 最大500MB程度

**気になる場合**:
```r
# 自動でサンプリング (分析内で実装)
if (nrow(data) > 10000) {
  data <- data[sample(1:nrow(data), 10000), ]
}
```

---

### Q: 日本語の列名でも動く?

**A:** 動きますが、**英数字推奨**です。

```
✅ 推奨: age, weight_kg, treatment
❌ 避ける: 年齢, 体重, 治療群

理由: 文字コード問題のリスク
```

---

### Q: 結果をPowerPoint/Wordに貼り付けたい

**A:** 現在の対応:

1. **テーブル**: 右クリック → コピー → Excel/Word に貼付
2. **グラフ**: スクリーンショット取得 → 貼付
3. **今後 (v2.1)**: 直接エクスポート機能

---

### Q: Rのパッケージインストールが必要?

**A:** オプショナルパッケージは**自動フォールバック対応**です。

- `pls` パッケージなし → PCA で代替実行
- `grf` パッケージなし → Ranger で代替実行
- `AER` パッケージなし → 手動2SLS で代替実行

**必須パッケージ** (自動で入っている):
- `survival` - 生存分析用
- Base R - 基本機能

---

## 🎯 推奨される使用例

### 医療・臨床研究者

```
Step 1: 患者データを CSV で整理
Step 2: 基本統計で患者背景を確認
Step 3: グループ比較で治療効果を検定
Step 4: 回帰分析で調整因子の影響を定量化
Step 5: 生存分析で長期予後を評価
```

### ビジネス・マーケティング

```
Step 1: 売上・顧客データを CSV で整理
Step 2: 基本統計で傾向を理解
Step 3: 回帰分析で価格・広告費の影響を定量化
Step 4: 時系列分析でトレンドを予測
Step 5: 次元削減で重要な顧客セグメントを発見
```

### 経済学・政策評価

```
Step 1: 政策前後のデータを CSV に整理
Step 2: パネルデータとして構築 (時系列 + 個体)
Step 3: 差分の差推定 (DiD) で政策効果を評価
Step 4: 因果推論で処置群と対照群を比較
Step 5: Causal Forest で異質効果を分析
```

---

## 💡 ベストプラクティス

### ✅ データ準備のTips

1. **列名は英数字のみ**
   ```
   ✅ age, bmi, systolic_bp
   ❌ 年齢, BMI, 収縮期血圧
   ```

2. **一貫した形式**
   ```
   ✅ 数値: 45, 123.4, -1.2
   ❌ 数値: 45 years, ~123.4, approx -1.2

   ✅ カテゴリ: M, F (または 1, 0)
   ❌ カテゴリ: Male, Female (大文字小文字混在)
   ```

3. **欠落値の処理**
   ```
   ✅ 完全に削除するか明示
   ❌ 空白セル（NULLなど記号なし）
   ```

4. **外れ値の確認**
   ```
   → 基本統計で最小値・最大値をチェック
   → 明らかに不自然な値は修正
   ```

### ✅ 分析のTips

1. **データの形状を理解してから開始**
   ```
   CSVをロード
   → 「列情報」タブで列数・型・サンプル値を確認
   → 期待通りか検証
   → 分析実行
   ```

2. **簡単な分析から始める**
   ```
   1. 基本統計で全体像を把握
   2. 適切なグループ比較を選択
   3. 回帰分析で関係性を定量化
   4. より高度な分析へ
   ```

3. **複数回実行して結果を確認**
   ```
   1回目: パラメータ確認
   2回目: 結果の整合性チェック
   3回目: レポート作成用に再実行
   ```

---

## 🆘 トラブルシューティング

| 問題 | 原因 | 解決方法 |
|------|------|--------|
| アプリ起動しない | R未インストール | `brew install r` |
| CSV読み込みエラー | エンコーディング問題 | CSVを UTF-8 で保存 |
| 分析失敗 | データ形式不適切 | CSV_FORMAT_GUIDE.md 確認 |
| 結果が表示されない | 出力フォーマット問題 | データサイズ/型を確認 |
| 遅い | 大規模データ | 1000行以上の場合は時間要 |

---

## 📚 さらに詳しく知りたい場合

| 内容 | 参照資料 |
|------|---------|
| CSVデータ形式 | `CSV_FORMAT_GUIDE.md` |
| 統計学の基本 | 右パネルの「解釈ノート」 |
| レシピ修正内容 | `IMPLEMENTATION_COMPLETE.md` |
| 技術詳細 | `RELEASE_READY_SUMMARY.md` |

---

## 🎉 まとめ

StatAppR はあなたの分析を**シンプル**で**高速**に実行します。

- **30個のレシピ**: 基本から高度な分析まで
- **7つのカテゴリ**: あなたのデータ型に最適な分析
- **サンプルデータ付き**: すぐに試せる
- **エラー対応**: わかりやすいメッセージ

**さあ始めましょう!** 🚀

---

**バージョン**: 2.0
**最終更新**: 2026-03-06
**言語**: 日本語・英語対応予定

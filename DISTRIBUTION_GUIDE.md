# StatAppR - 配布とテストガイド

## 配布パッケージ

**ファイル**: `StatAppR.tar.gz` (637 KB)

## 別の Mac でのセットアップ手順

### 1. アプリケーションの展開と配置

```bash
# tar.gz を展開
tar -xzf StatAppR.tar.gz

# Applications フォルダに移動
mv StatAppR.app /Applications/
```

### 2. R のインストール（初回起動時）

**オプション A: アプリ内での自動インストール（推奨）**
1. StatAppR を起動
2. レシピを選択すると「Rが見つかりません」ダイアログが表示
3. 「今すぐインストール」をクリック
4. Homebrew 経由で R が自動インストールされる

**オプション B: 事前インストール**
```bash
brew install r
```

### 3. テスト手順

#### Step 1: アプリケーション起動
```bash
open /Applications/StatAppR.app
```

#### Step 2: CSV ファイルを読み込み
1. "📁 CSVファイルを選択" をクリック
2. サンプルデータから 1 つ選択：
   - `1_BasicStatistics_heights.csv` - 身長統計
   - `2_GroupComparison_treatment_vs_control.csv` - グループ比較
   - `3_Correlation_study_data.csv` - 相関分析
   - その他

#### Step 3: データ型を確認
- CSV が自動的に分類される
- 推奨レシピが表示される

#### Step 4: レシピを実行
1. レシピを選択
2. パラメータをチェックボックスで選択
3. 「分析を実行」をクリック
4. 結果が表示される

## テスト対象機能

### ✅ 実装済み機能

| 機能 | 説明 | テスト方法 |
|------|------|---------|
| **CSV 読み込み** | CSV ファイルのインポート | CSVを選択して確認 |
| **データ型判定** | 自動カテゴリ分類 | CSVロード後の分類を確認 |
| **レシピ実行** | R バックエンド実行 | "分析を実行"をクリック |
| **パラメータ選択** | チェックボックス UI | パラメータセクション確認 |
| **結果表示** | 統計結果の表示 | 実行後の結果を確認 |
| **R 自動インストール** | Homebrew 経由でのインストール | "Rが見つかりません"時に実行 |
| **パッケージ管理** | パッケージインストール UI | "🔧 パッケージ管理"を確認 |

## 期待される動作

### 正常な動作フロー

```
アプリ起動
  ↓
CSV を選択
  ↓
データ型が自動判定される
  ↓
対応するレシピが推奨される
  ↓
レシピを選択
  ↓
パラメータをチェックボックスで選択
  ↓
"分析を実行"をクリック
  ↓
R が実行される
  ↓
結果が表示される
  ✅ 成功
```

## トラブルシューティング

### 1. "Rが見つかりません" ダイアログが表示される
**解決策**:
- 「今すぐインストール」をクリック
- または手動で `brew install r` を実行

### 2. "ファイルが破損している可能性があります" エラー
**解決策**:
```bash
# セキュリティ設定を許可
xattr -d com.apple.quarantine /Applications/StatAppR.app
```

### 3. レシピ実行後に "エラー" メッセージが表示
**確認事項**:
- CSV ファイルが正しい形式か
- 選択した列が存在しているか
- R がインストールされているか

## 推奨テスト環境

- **OS**: macOS 15.6 以上
- **メモリ**: 2 GB 以上
- **ディスク空き容量**: 500 MB 以上

## テスト用サンプルデータ

アプリに同梱されているサンプルデータ（7 ファイル）:

1. **BasicStatistics** - 身長・体重の基本統計
2. **GroupComparison** - 処置群 vs 対照群の比較
3. **Correlation** - 複数変数の相関分析
4. **Survival** - 患者追跡（生存分析）用
5. **RegressionData** - 回帰分析用
6. **TimeSeriesData** - 時系列データ
7. **MetaAnalysis** - メタアナリシス用

## テスト完了後

- 動作結果を報告
- 問題があれば詳細を記録
- スクリーンショットを共有（重要な場合）

## 技術仕様

| 項目 | 詳細 |
|------|------|
| **言語** | Swift (SwiftUI) + R |
| **ビルドサイズ** | 637 KB (圧縮) |
| **実行サイズ** | 約 30-50 MB (インストール後) |
| **対応 R バージョン** | 4.0 以上 |
| **R パッケージ** | 30+ 統計パッケージ |
| **レシピ数** | 30 個 |
| **対応データ型** | 7 種類（自動判定） |

---

質問や問題があれば、詳細情報を集めてお知らせください。
